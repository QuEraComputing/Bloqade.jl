name: TagBot
on:
  issue_comment:
    types:
      - created
  workflow_dispatch:
jobs:
  TagBot:
    if: github.event_name == 'workflow_dispatch' || github.actor == ''
    runs-on: [self-hosted]
    steps:
      - uses: JuliaRegistries/TagBot@v1
        with:
          token:
